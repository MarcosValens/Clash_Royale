export function Card(count, iconUrl, id, level, maxLevel, name) {
    this.count = count
    this.iconUrl = iconUrl
    this.id = id
    this.level = level
    this.maxLevel = maxLevel
    this.name = name
}
